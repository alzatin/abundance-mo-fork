import { clamp as _clamp } from "@thi.ng/math/interval";
import { defHofOp } from "./compile/emit.js";
const [clampN, clampN2, clampN3, clampN4] = defHofOp(_clamp, ([o, a]) => `${o}=op(${a},n,m);`, "o,a,n,m", "o,a");
export {
  clampN,
  clampN2,
  clampN3,
  clampN4
};
