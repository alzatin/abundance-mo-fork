import { defOp } from "./compile/emit.js";
import { ARGS_V } from "./compile/templates.js";
const [exp_2, exp_22, exp_23, exp_24] = defOp(
  ([o, a]) => `${o}=Math.pow(2,${a});`,
  ARGS_V
);
export {
  exp_2,
  exp_22,
  exp_23,
  exp_24
};
