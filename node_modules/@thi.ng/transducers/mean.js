import { reduce } from "./reduce.js";
function mean(xs) {
  let n = 1;
  return xs ? reduce(mean(), xs) : [
    () => n = 0,
    (acc) => n > 1 ? acc / n : acc,
    (acc, x) => (n++, acc + x)
  ];
}
export {
  mean
};
