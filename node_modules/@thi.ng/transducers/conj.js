import { reduce, reducer } from "./reduce.js";
function conj(xs) {
  return xs ? reduce(conj(), xs) : reducer(
    () => /* @__PURE__ */ new Set(),
    (acc, x) => acc.add(x)
  );
}
export {
  conj
};
