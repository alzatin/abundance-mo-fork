import { reduce, reducer } from "./reduce.js";
function assocObj(xs) {
  return xs ? reduce(assocObj(), xs) : reducer(
    () => ({}),
    (acc, [k, v]) => (acc[k] = v, acc)
  );
}
export {
  assocObj
};
