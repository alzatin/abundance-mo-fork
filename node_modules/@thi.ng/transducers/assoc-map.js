import { reduce, reducer } from "./reduce.js";
function assocMap(xs) {
  return xs ? reduce(assocMap(), xs) : reducer(
    () => /* @__PURE__ */ new Map(),
    (acc, [k, v]) => acc.set(k, v)
  );
}
export {
  assocMap
};
